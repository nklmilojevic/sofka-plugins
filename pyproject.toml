[project]
name = "sofka-plugins-dev"
version = "0.0.0"
description = "Local tools for the Sofka plugin catalog"
requires-python = ">=3.12,<3.13"
dependencies = [
  "blake3==1.0.9",
  "zstandard==0.25.0",
]

[tool.uv]
package = false
